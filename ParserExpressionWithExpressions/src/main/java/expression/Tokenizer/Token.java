package main.java.expression.Tokenizer;

public enum Token {
    NOT, COUNT, NUMBER, VARIABLE, NEGATIVE, SUB, ABS, SQUARE, ADD, DIV, MUL, MOD, LEFT_SHIFT, RIGHT_SHIFT,
    OPEN_BRACE, CLOSE_BRACE, EOF, MISTAKE, XOR, AND, OR
}
